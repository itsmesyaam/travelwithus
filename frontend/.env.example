# No environment variables are strictly required for TravelWithUs frontend Vercel deployment.
# All production API requests are dynamically proxied via vercel.json configuration settings.
# 
# (Optional) If you integrate frontend-specific client services in the future:
# VITE_GOOGLE_MAPS_API_KEY=your_google_maps_key
